# kessan-tool（決算ツール）作業ルール

決算の1枚画像・note記事・YouTube横動画を作るツール。本体は `index.html` の1ファイル（中に `<script>`）、サーバ側は `functions/`。

## タスク管理

### 起票先

- このツールの**開発タスク・バグは、Notion の「📊 決算ツールPJ ＞ 決算ツールPJタスク」DB に起票する**。
  - PJページ: https://app.notion.com/p/3e24a4a7eb9f813b90dbf4a7676ffd6c
  - タスクDB: https://app.notion.com/p/1870b9779e73447baee73bed2519d947
- **1 依頼 = 1 タスク**。1つの依頼に複数の指摘が混ざっていたら、指摘ごとに分けて起票する（「あとでまとめて」にしない）。
- 他のDBに入れない。切り分けは次のとおり。
  | 内容 | 置き場所 |
  |---|---|
  | このツールの機能・バグ | 📊 決算ツールPJタスク |
  | 動画の企画・投稿予定などチャンネル運用 | Youtubeタスクリスト ＞ タスクリスト |
  | ブログ（sumikko-money）側 | 📈 すみっこマネー大学PJタスク |
  | ナレスラ本体 | 🎬 動画ツール収益化PJタスク |

### 起票するとき

- 勝手にDBやページを新しく作らない。置き場所に迷ったら、先に聞く。
- プロパティは分かる範囲で埋める：ステータス（起票／対応中／レビュー待ち／完了 ほか）、タスク種別、カテゴリ、ボール（Claude／すみぽん）、作業場所、優先度。
- 本文には「やること」「いまの状態（どのファイル・どの関数か）」を書く。実装前の想像で手順を長々と書かない。

### 進めるとき

- 着手したら `対応中`、こちらの作業が終わったら `レビュー待ち`。ボールは作業の持ち主を指す（レビュー待ちでも、直すのが Claude なら Claude のまま）。
- 終わったら `完了` にして完了日を入れ、何をしたかを「実装記録」に1〜3行で残す。

### Notion とリポジトリの使い分け

- **Notion**＝「何をやるか・どこまで進んだか」。
- **リポジトリの `docs/`**＝「どう作ったか・なぜそうしたか」。実装の経緯は `docs/impl/`、設計・方針は `docs/design/` に日付つきで残す。
- 同じ内容を両方に長く書かない。Notion のタスクからは、必要なら docs のファイル名を指す。

## 作業の進め方

- 原因を追う変更と、別件の指示は、コミットを分ける（混ぜると、どの変更が効いたか分からなくなる）。
- AIプロンプトは、出力が悪くなったら「足す」より先に「原因になった変更を戻す」。
- 自動テストは無い。`index.html` を触ったら `<script>` を取り出して `node --check` で構文だけは必ず確認する。
- ユーザー向けの文言は、やさしい日本語で書く（専門用語をそのまま出さない）。
