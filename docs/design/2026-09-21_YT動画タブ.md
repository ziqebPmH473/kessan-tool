# 設計書：「🎬 YT動画」タブ（テーマを問わない横動画）

作成日：2026-09-21（`4a754ea` 時点で作成。`5d08bf8`＝画面 v20260921-2327 のコードに合わせて見直し済み。差分は §14）
状態：実装済み（2026-09-21 深夜）。実装の記録と、設計から変えた点は [../impl/2026-09-21_YT動画タブ.md](../impl/2026-09-21_YT動画タブ.md)。プロンプトの文面は、実際に1本作りながら直す。
関連：[2026-09-20_YouTube横動画.md](2026-09-20_YouTube横動画.md)、[../impl/2026-09-21_YouTube横動画.md](../impl/2026-09-21_YouTube横動画.md)、[../prompts/gem-youtube-rules.md](../prompts/gem-youtube-rules.md)

## 1. 目的

株価分析の横動画の仕組み（音声 → スライド → カット → ナレスラ → 投稿文）を、テーマを問わない動画にも使えるようにする。対象は、すみっこマネー大学で出す動画。

- 一番上のタブの「📉 株価分析」の右に「🎬 YT動画」を足す。
- 調査タブだけ中身を差し替え、音声以降は同じ画面を使う。
- 最後に、note（無料・YouTubeを差し込む）とブログの記事データを作るタブを足す。

## 2. ユーザーと決めたこと

| 項目 | 決定 |
|---|---|
| 対象の範囲 | すみっこマネー大学の別テーマまで。チャンネルに依存する文言は設定欄に出す |
| 調査の流れ | テーマを入力 → ChatGPTと会話して出したレポートを貼る（形は自由・プロンプトなし）→ 「①構成と出典を付ける」プロンプトを投げて結果を貼る → ②ファクト／③再CK／④修正を繰り返す → 音声以降は株と同じ |
| ①の出力 | レポートの本文は書き換えない。先頭に構成の表、本文に［出典n］の印、末尾に出典一覧、の3つだけ足す |
| ①を投げる先 | 調べた会話と同じチャットを勧める（実際に見た資料のURLが出る）。プロンプトにはレポート全文を差し込み、別チャットでも使える形にする。ファクトチェックは今と同じく別チャット |
| データの持ち方 | 株の横動画と YT動画で、1本ずつ別に持つ。過去分はエクスポートで残す |
| 記事の元データ | ファクトチェック済みの動画資料。Gemini API とプロンプトコピーの両方。YouTubeのURLは後から欄に入れて差し込む。noteは無料記事 |
| 最後のスライドの note案内＋QR | QR用のURLがあるときだけ付ける |
| チャート（TradingView）の差し込み | 残す（AIがティッカーを出したときだけ） |
| サムネ | 3段（Gemのルールどおり）。中段は**この動画の主題**（下記） |

### サムネの中段（ユーザー指摘 2026-09-21）

- 中段は、動画の中の注目点ではなく、**その動画の主題**（何が分かる動画か・何を分析する動画か）を、視聴者が見たくなる言葉で書く。6〜9文字。
- 株の横動画で中段が銘柄名なのは、銘柄名が「何の動画か」を表すから。YT動画ではテーマがその役割。
- 動画の中で取り上げる個別の材料・指標・要因は、中段に書かない（使うなら上段・下段の側）。
- 上段（感情・問題提起）と下段（抽象的な問い）は、Gemの原文の役割のまま、中段の主題に向けて作る。
- プロンプトには「この動画の主題：（テーマ欄の内容）」を明示して渡す。動画資料から推測させない（資料の中で目立つ数字や用語が中段に来やすくなる）。
- ユーザーが挙げた例（半導体関連株の次に来るテーマ）は理解の確認用。プロンプトには例を入れない。

## 3. 技術判断で決めた前提

- **音声タブ以降のUIと関数は、株価分析とYT動画で共通の1組**。今あるものをそのまま使う。片方で直したことは、両方に効く。
- YT動画で違う箇所は、同じ関数の中で「株か・YT動画か」を見て分ける。新しく作るのは、調査タブ（テーマ・レポート・①構成と出典）と記事タブだけ。分ける箇所は §7-3〜7-5 に挙げたものに限り、勝手に増やさない。
- 分けた箇所（プロンプトの文言など）を直すときは、もう片方にも同じ直しが要るかをその都度確認する。
- 横動画の画面と関数（約160個）は複製しない（IDが固定で、複製すると二重管理になる）。
- 動画の種類を `YT_CTX`（`'stock'` か `'gen'`）で持ち、切り替えで部品と保存先を入れ替える（`thCtxSync` と同じ考え方）。
- 株側の IndexedDB のキーと localStorage の欄は今のまま。既存データの移行は要らない。
- 新しい部品のIDは `yg-` で始める。

## 4. 現状

- 横動画の左側は `#mode-price` の動画タブの中（`#vd-orient-y`）に1組だけ。右ペインは `#capture-area-price` の中で `setMode` で切り替える。
- 保存：入力欄は localStorage（`YT_PERSIST_IDS`）。音声とカットは IndexedDB `kessanTool-ytAudio`（`audio` `state`）。スライドなどは `kessanTool-ytNare`（`slides` `plan` `tpl` `srcimg` `srcedit`）。読み書きは `yaPut/yaGet`・`ynPut/ynGet` に集まっている。エクスポートは `hidden.yt`。
- 株に依存している関数・定数：`ytResearchPrompt`／`ytFactCheckPrompt`（「レポート1〜2章は省略」の文）／`ytFreeReport`（6か所）／`YT_CHAPTERS`（3か所）／`ytIntent`／`vdBasicInfo('vd')`（4か所）／`getDir()` の出し分け／`getName()`・`getTicker()`（横動画の範囲で約20か所。プロンプトの「銘柄：」行、書き出すファイル名7か所、読みがな、投稿文の組み立て、`ynTicker`、音声の持ち主の確認）／読みがなの取り出し／`YT_SLIDE_DESIGNER`／`YT_POST_RULES` 末尾の読み替え／出典のチャート種別／QR用のURL。
- movie のPJで流用できるもの：ニュース用の `analysis`（根拠資料をレポート形式で）と音声ルール一式。音声ルールはすでに `YT_VOICE_RULES` に入っている。
- ブログの貼り付け形式：【タイトル】【本　文】【ハッシュタグ】の3ブロック（`sumikko-money/docs/article-format.md`）。note用と同じ形で、表が使える点だけ違う。

## 5. 影響範囲

- `switchMode`、保存と復元（`saveState`、`ui.mode`）、エクスポート／インポートのタブ一覧。
- 横動画の関数のうち、銘柄名・章・意図・保存キーを使う箇所。株側は `YT_CTX==='stock'` のとき今と同じ結果にする。
- `thCtxSync` に3つ目の保存先。
- `functions/` は変更しない。

## 6. 変更するファイル／しないファイル

- 変更：`index.html`、`docs/impl/` の実装記録（完了時）。
- 変更しない：`functions/api/*`、`cut-test.html`、`tts-duo-test.html`、`docs/prompts/gem-youtube-rules.md`（原文のまま使い、読み替えだけ足す）、movie／sumikko-money のリポジトリ。

## 7. 詳細設計

### 7-1. 動画の種類の切り替え（土台）

- `let YT_CTX='stock'`、`const ytGen=()=>YT_CTX==='gen'`。
- IndexedDB：`yaPut/yaGet/ynPut/ynGet` の中でキーを `ytKey(k)` に通す。gen は `'gen:'+k`、stock はそのまま。`tpl`（ナレスラのひな形）は共通で、接頭辞なし。
- 入力欄：`YT_PERSIST_IDS` のうち、設定タブの共通項目（`yt-p-fixed` `ya-ctx` `yt-months` `yt-nlm-min`、Geminiのモデル `yt-m-cut` `yt-m-read` `yt-m-plan` `yt-m-post` `yt-m-qr`）以外が「作品ごとの欄」。切り替え時に、今の値を `localStorage['kt-yt-fields-'+旧ctx]` に退避し、新しい ctx の値を欄に入れて `saveState()`。
- メモリ上の状態（`YA` `YN` `YS`）：切り替え前に保留中の保存を流す（`_yaSaveTimer` を即時実行する `yaFlush()` を足す）。そのあと、起動時・インポート時と同じ読み直しを呼ぶ。再生中の音声は止める。
- SNSの定型文（`yt-f-yp` `yt-f-ig` `yt-f-l8` `yt-f-tt`）は作品ごとではなく**種類ごと**に持つ（株用と YT動画用で文面が違うため。§7-5）。
- 保存済みの音声が別の作品のものかを知らせる仕組み（`YA` の state の `stock`＝銘柄名）は、gen ではテーマで比べる。
- サムネイル：`th-ctx` に `gen` を足す。保存先は `yg-th-text`／`yg-th-preset`。

### 7-2. タブと画面

- `.mode-tabs` に `<button data-mode="yt">🎬 YT動画</button>`。左側の空の入れ物 `#mode-yt`。
- `switchMode('yt')`：横動画のパネル（`ytBuildTabHtml` の出力を包む要素に `id="yt-root"`）を `appendChild` で `#mode-yt` に移す（値やイベントは残る）。右側は `#capture-area-price` を表示し `ytPaneMode()` の表示に。「スライド」「動画（縦）」のタブボタンは隠す。`YT_CTX='gen'`。
- ほかのモードへ出るとき：パネルを `#vd-orient-y` に戻し、`YT_CTX='stock'`、右ペインのボタンを元に戻す。
- 内側タブ：調査／音声／スライド／カット／ナレスラ／投稿文／記事／設定（「記事」は gen のときだけ）。

### 7-3. 調査タブ（gen 用の別パネル `yg-pane-research`）

1. テーマ欄 `yg-theme`（1行）。シリーズ名は今の `yt-series`（gen の既定値は空）。
2. レポートの貼り付け欄 `yg-report`（形は自由。📋貼り付けボタン）。
3. ボタン：💬 ①構成と出典を付ける（`ygStructPrompt`）／✅ ②ファクト／🔁 ③再CK／🔧 ④修正。
4. 動画資料の貼り付け欄は `yt-doc` を共用（作品ごとの欄）。貼ると②を自動でコピー。🔗出典のリンク確認は今のまま。

①のプロンプトの骨子（方向はここで合意済み。文面は実装時に決めて入れ、結果で直す）：
- 役割は構成作家。目的に `ytIntent()`。
- 本文は書き換えない。足すのは、先頭の `## 構成` の表（章番号｜章の名前｜扱う内容）、本文の `［出典n］` の印、末尾の `## 出典一覧` の3つだけ。
- 出典一覧の列、出力の形、PDFの扱いの文は、今の調査プロンプトから流用。
- 先頭に、読み間違えやすい言葉の読みがなの欄。
- 「同じチャットに投げる」という案内は、プロンプトではなく画面の注記に書く。

②ファクトチェック：`ytFactCheckPrompt` を ctx で分ける。gen では「レポートは確認済みなので省く」の段を外し、構成の表も確認対象。禁止事項の列挙は、売買を促す表現と視聴者への行動要求だけ。

### 7-4. 構成の読み取りと差し替え

- `ytChapters()`：stock は `YT_CHAPTERS`、gen は `yt-doc` の `## 構成` の表を読んで `[{no,name,what}]`。読めないときは空の配列と画面の注意。
- `YT_CHAPTERS` を直接使っている3か所を `ytChapters()` に置き換える。スライドの枚数の文言は `章の数＋1` から作る。
- gen の扱い：
  - `ytIntent()`：`「テーマ」を解説する動画（シリーズ名があれば付ける）`
  - `vdBasicInfo('vd')` の4か所：`テーマ：…` の1行
  - `ytFreeReport()`：`fr.ok` が false の道を通す。throw している箇所だけ分岐
  - 読みがな：動画資料の「読みがな」欄から。欄の見出しは「読み間違えやすい言葉」
  - `getName()` を使う表示・ファイル名・投稿文：`ytTitleName()`（gen はテーマ、stock は銘柄名）。ファイル名は `ytFileBase()` にまとめる（stock は今の `コード_銘柄名`、gen はテーマ）。対象：スライド作成一式ZIP／YouTube資料.md／音声_カット済み／文字起こし.txt／割り当て用_番号入りスライド.zip／.svpz とその中の name
  - プロンプトの「銘柄：名前（コード）」の行（スライド・投稿文）：gen は「テーマ：…」
- `YT_SLIDE_DESIGNER`：冒頭の役割の一文と、チャート・まとめ（QR）の段を差し込み式に。QRの段はQR用のURLがあるときだけ。
- 演出の割り当てのチャートは gen でも残す（AIがティッカーを出したときだけ）。`ynTicker()`（証券コードから `TSE:` を作る）は gen では使わず、AIが出したティッカーをそのまま使う。
- 番号入りのスライド画像・取り込み時の整え（`ynFxTidy`）・読み取りの「続きから」・モデルの設定は、株に依存していないのでそのまま共通で使う。

### 7-5. 投稿文

- `YT_POST_RULES` の本文（Gemの原文）はそのまま。末尾の【この動画での読み替え】だけ gen 用を用意。
- サムネは3段、記法 `上段：中段：下段` と色マーカーはそのまま。中段は §2「サムネの中段」のとおり。
- タグ：「銘柄名と証券コードは必ず」の文を外す。
- SNSは5つ（X・YouTubeの投稿・Instagram・Lemon8・TikTok）。部品（hook・points・x・lemon8_title・lemon8_intro とタグ）をツールが組み立てる今の作り（`ytPostBuild`）をそのまま使う。方針は [2026-09-21_SNS投稿文.md](2026-09-21_SNS投稿文.md)。
  - `【#銘柄名（コード）】` は、X の先頭行と Lemon8 の見出しの2か所。gen ではどちらも付けない。銘柄名・コードのタグを本文から抜く処理も gen では動かさない。
  - 読み替えの「hook に銘柄名を入れる」は、gen では「動画の主題が分かる言葉を入れる」。
  - 定型文 `YT_SNS_FIXED` は「株価の下値／上値や今後のシナリオは note で」という株の有料記事の案内なので、gen 用の既定を別に持つ（動画への誘導と、無料の note 記事の案内）。`{下値/上値}` の入れ替えは gen では行わない。
- 概要欄の「▼ さらに詳しい分析はこちら（note）」は、gen では無料記事のURL（記事タブを作ったあとに入れる）を指す。
- 投稿文を Gemini API で作ったあとの自動バックアップ（`exportTabData('price')`）は、gen では `exportTabData('yt')` にする。

### 7-6. 記事タブ（gen のみ）

- 元データ：ファクトチェック済みの `yt-doc`（構成の表・出典の印・出典一覧を除いた本文）。
- ボタン：note用・ブログ用のそれぞれに「プロンプトをコピー」「🤖 Gemini APIで作る」。
- 出力：【タイトル】【本　文】【ハッシュタグ】の3ブロック。note用は表を禁止、ブログ用は表を使う。
- YouTube のURLは `yt-p-yturl`。note用は冒頭の段落のあとにURLを単独の段落で（貼ってEnterでカードになる）。ブログ用は `[動画を見る](URL)`（埋め込みの形は sumikko-money 側を確認してから決める）。
- URLが空でも本文は作れ、あとから差し込み直せる。差し込みは何度押しても増えない（関連記事の挿入と同じ）。
- 既存の売買表現チェックを通す。

### 7-7. エクスポート／インポート

- タブ一覧に `yt:{label:'YT動画',roots:['#mode-yt'],nameId:'yg-theme'}`。
- `hidden.yt` は今の形のまま。gen の状態で書き出して gen に戻す。ファイルに `ctx` を記録し、違うタブで読もうとしたら止めて知らせる。

## 8. 実装手順

1段階＝1コミット。各段階のあとに `node --check` と株側の回帰確認。プロンプトは、この設計書にない方向の変更だけ先に相談する。文面の確認では止まらず、入れたあとに変更一覧を返す。
コミットの前に `git status` で自分が触ったファイルだけか確かめ、`git add <ファイル>` で入れる（`git commit -a` を使わない）。プッシュの報告は画面のバージョン（v20260921-2327 の形）で行う。

1. 土台：`YT_CTX`、`ytKey`、欄の退避と復元、`yaFlush`、読み直し（見た目は変えない）
2. タブの追加、パネルの移動、右ペインの出し分け、`thCtxSync` の3つ目
3. 調査タブ（gen）：テーマ、レポート、①〜④
4. `ytChapters()` ほか、gen 用の差し替え（音声・スライド・カット・ナレスラ）
5. 投稿文の読み替え（gen）
6. 記事タブ
7. エクスポート／インポート
8. `docs/impl` の実装記録、Notion の更新

## 9. テストケース

| No | 種別 | 確認内容 | 実行方法 | 期待結果 |
|---|---|---|---|---|
| 1 | 回帰 | 株の横動画の各プロンプト | 変更前後で ①調査・②ファクト・NotebookLM・スライド・割り当て・投稿文をコピーし、文字列を比べる | 完全に一致 |
| 2 | 回帰 | 株側の保存済みデータ | 既存の状態で開き直す | 音声・カット・スライド・割り当て・出典画像が今までどおり |
| 3 | 正常 | データが混ざらない | 株で作業中に YT動画へ移り、別の音声とスライドを入れて、株に戻る | どちらも自分のデータだけ。リロード後も同じ |
| 4 | 正常 | 保留中の保存 | カットを変えた直後（1秒以内）にタブを切り替える | 変更が元の側に保存され、相手側に書かれない |
| 5 | 正常 | 構成の読み取り | 5章・9章の表を貼る | スライド名、枚数の文言、NotebookLMの章の順が一致 |
| 6 | 異常 | 構成の表が無い・崩れている | 表なしの資料を貼る | 注意が出て、スライドのプロンプトは作れないと知らせる。落ちない |
| 7 | 異常 | テーマ／レポートが空で①を押す | ①を押す | やさしい文で知らせる |
| 8 | 正常 | 出典一覧、リンク確認、出典の画像 | gen の資料で実行 | 株と同じに動く |
| 9 | 正常 | QR | URLあり／なしで、スライドのプロンプトとまとめスライドを見る | ありのときだけQRの段と貼り付けが出る |
| 10 | 正常 | サムネ | 投稿文を作り「サムネに入れる」 | 3段で、中段が動画の主題。note用・株のYouTube用の文言は変わらない |
| 11 | 正常 | 記事 | note用とブログ用を作り、URLを後から入れて差し込み直す | 3ブロックの形。noteは表なし。URLは1か所だけ |
| 12 | 正常 | エクスポート／インポート | gen で書き出し、IndexedDB を消して読み込む | 同じ状態に戻る。株側は変わらない |
| 13 | 異常 | 株のファイルを YT動画で読む（逆も） | 読み込む | 止めて知らせる |
| 14 | 環境 | Gemini API のボタン | 本番で押す（ローカルにはキーが無い） | カット判定・割り当て・投稿文・記事が返る |
| 15 | 目視 | タブ4つの表示 | 狭い幅で見る | 折り返しても崩れない |

テストの進め方：`<script>` を取り出して `node --check` → ローカルの wrangler（`http://localhost:8799`）→ No.1 は変更前のコミットで各プロンプトを保存し、変更後と `diff` → 本番で No.14。

## 10. 既存仕様を壊すリスク

- **切り替え時の保存の取り違え**がいちばん大きい（保留中のタイマー、非同期の読み込み中の切り替え）。切り替えは「保存を流す → ctx を変える → 読み直す」を1つの `async` 関数にまとめ、処理中はタブを押せなくする。
- パネルを移すことで、`#mode-price` 配下を前提にした処理（エクスポートの `roots`、`ytSyncTabs`）がずれる可能性。
- `thCtxSync` が2つの保存先を前提にしている箇所。

## 11. 注意点

- ブログでの YouTube の埋め込みの形は、sumikko-money 側で未確認。記事タブの着手時に確認する。
- 起票済みの「ブログ記事用のデータ出力」タスクとは対象が違う（あちらは株の記事）。
- 別セッションも同じフォルダで `index.html` を直す。着手時に `git log` を見て、§14 のあとに入った変更があれば、もう一度この設計書と突き合わせる。

## 12. やってはいけないこと

- 横動画の関数・画面を複製する
- 株側の保存キーの名前を変える
- Gemの原文を書き換える
- この設計書にない方向へプロンプトを相談なしに変える／許可の一文や例をプロンプトに足す
- 複数の段階を1コミットに混ぜる
- ほかのDBに起票する

## 13. 実装フェーズに渡す指示文

この設計書の設計・実装手順・テストケース通りに、最小差分で実装〜テストを実施してください。§8の順に1段階1コミットで進めてください。プロンプトは設計書の方向どおりに文面を決めて入れ、変更一覧を返してください。設計と異なる実装が必要になった場合は、勝手に進めず、設計差し戻しメモを作成してください。

## 14. 見直しの記録（2026-09-21 深夜、`5d08bf8`＝v20260921-2327 に合わせた）

設計を書いたあと、横動画（株価分析）側に15コミット入った。設計の骨組み（画面と関数は共通の1組・保存先だけ分ける）は変えていない。合わせた点：

| 変わったこと | 設計への影響 |
|---|---|
| 投稿文が X だけから SNS 5つに（部品をツールが組み立てる。定型文 `yt-f-*` を設定タブに追加） | §7-5 を書き直し。`【#銘柄名（コード）】` は2か所、定型文は種類ごとに持つ |
| Gemini のモデルを作業ごとに設定（`yt-m-*`） | §7-1 の共通の設定に追加 |
| 投稿文をAPIで作ったら株価分析のバックアップを書き出す | §7-5：gen では YT動画のバックアップにする |
| 番号入りスライド画像のZIP（ファイル名に銘柄名） | §7-4：ファイル名を `ytFileBase()` にまとめる対象に追加 |
| 割り当ての見直し①②③、`ynFxTidy`、読み取りの続きから | 株に依存しないので共通のまま。設計の変更なし |
| 保存キー（IndexedDB）・`YT_CHAPTERS` の使用箇所（3か所） | 変わっていない |
| 進め方のルール（プロンプトは方向だけ相談／`commit -a` を使わない／報告は画面のバージョン） | §7-3・§8 を直した |
| `getName()` などの使用箇所 | 「約100か所」は数え間違い。横動画の範囲では約20か所（§4） |

